rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Single iOS write at trip completion: FirebaseService.writeTripCompletionToFirebase (PATCH = create-or-merge).
    // Same key list for create and survey-open update. Extra iOS fields must be added here first, then deployed.
    function mobileTripWriteKeys() {
      return [
        'tripId', 'destinationCode', 'timezonesCount', 'travelDirection',
        'pointsCompleted', 'startDate', 'completionDate', 'completionMethod',
        'point1Completed', 'point2Completed', 'point3Completed',
        'point4Completed', 'point5Completed', 'point6Completed',
        'point7Completed', 'point8Completed', 'point9Completed',
        'point10Completed', 'point11Completed', 'point12Completed',
        'surveyCompleted', 'arrivalTimeZone', 'originTimezone', 'originTimeZone',
        'useCrampedPoints', 'surveyCode', 'deviceId',
        'surveyStatus', 'researchConsentGranted',
        'point1Timezone', 'point2Timezone', 'point3Timezone',
        'point4Timezone', 'point5Timezone', 'point6Timezone',
        'point7Timezone', 'point8Timezone', 'point9Timezone',
        'point10Timezone', 'point11Timezone', 'point12Timezone'
      ];
    }

    // In-app survey PATCH (iOS SurveyView / FirebaseService.submitSurveyResponse; legacy web survey removed).
    function surveySubmissionKeys() {
      return [
        'surveyCompleted', 'surveySubmittedAt', 'surveyStatus',
        'userComment', 'ageRange',
        'generalAnticipated', 'sleepPost', 'fatiguePost', 'concentrationPost',
        'irritabilityPost', 'motivationPost', 'giPost',
        'tripId', 'surveyCode', 'created'
      ];
    }

    // Post-completion survey edits: survey answers only (no trip metadata rewrites).
    function surveyEditKeys() {
      return [
        'surveyCompleted', 'surveySubmittedAt', 'surveyStatus',
        'userComment', 'ageRange',
        'generalAnticipated', 'sleepPost', 'fatiguePost', 'concentrationPost',
        'irritabilityPost', 'motivationPost', 'giPost'
      ];
    }

    // Initial survey submit: if tripId is written, it must match the document id.
    function surveySubmitShapeOk(tid) {
      let affected = request.resource.data.diff(resource.data).affectedKeys();
      return affected.hasOnly(surveySubmissionKeys())
        && (!affected.hasAny(['tripId']) || request.resource.data.tripId == tid);
    }

    // Post-completion survey edits: only answer fields.
    function surveyEditShapeOk() {
      let affected = request.resource.data.diff(resource.data).affectedKeys();
      return affected.hasOnly(surveyEditKeys());
    }

    // One mobile full write while survey open; after that only survey fields (submit or edit answers).
    function tripCompletionUpdateAllowed(tid) {
      let affected = request.resource.data.diff(resource.data).affectedKeys();
      let surveyNotDone = resource.data.surveyCompleted != true;
      return (surveyNotDone && affected.hasOnly(mobileTripWriteKeys()))
        || (surveyNotDone
            && request.resource.data.surveyCompleted == true
            && surveySubmitShapeOk(tid))
        || (resource.data.surveyCompleted == true && surveyEditShapeOk());
    }

    // New docs: HMAC-signed 5-part id (AppState / HMACGenerator). Legacy reads unchanged.
    function signedTripIdCreate(tid) {
      return request.resource.data.tripId == tid
        && tid.matches('^[A-F0-9]{8}-[A-Z]{4}-[0-9]{6}-[0-9]{4}-[a-f0-9]{8}$');
    }

    function tripCompletionCreateAllowed(tid) {
      return signedTripIdCreate(tid)
        && request.resource.data.keys().hasOnly(mobileTripWriteKeys());
    }

    // IRB approved (2026-06): production trip creates allowed with signed 5-part id.
    match /tripCompletions/{tripId} {
      allow read: if true;
      allow create: if tripCompletionCreateAllowed(tripId);
      allow update: if tripCompletionUpdateAllowed(tripId);
      allow delete: if false;
    }

    match /tripCompletionsDev/{tripId} {
      allow read: if true;
      allow create: if tripCompletionCreateAllowed(tripId);
      allow update: if tripCompletionUpdateAllowed(tripId);
      allow delete: if false;
    }

    match /auditLog/{entry} {
      allow read: if true;
      allow write: if false;
    }

    match /_system/{doc} {
      allow read: if true;
      allow write: if false;
    }

    match /_snapshots/{doc} {
      allow read: if true;
      allow write: if false;
    }

    // Product usage sessions (iOS) — one doc per app process open; append mid-session.
    // Doc id: {device8}-{YYMMDD}-{HHMMSS} (same 8-char research device suffix as trip ids).
    // App: ProductUsageAnalytics.swift. Deploy before shipping builds that write sessions.
    function iosUsageSessionKeys() {
      return [
        'sessionId', 'deviceId', 'platform', 'appVersion', 'openedAt', 'events', 'lastEventName',
        'destinationCode', 'originCode', 'outcome', 'pointsBucket', 'durationBucket',
        'completionMethod', 'researchDecision'
      ];
    }

    function iosUsageSessionIdOk(sid, deviceId) {
      return sid is string
        && deviceId is string
        && deviceId.matches('^[A-F0-9]{8}$')
        && sid.matches('^[A-F0-9]{8}-[0-9]{6}-[0-9]{6}$')
        && sid.matches(deviceId + '-[0-9]{6}-[0-9]{6}');
    }

    function iosUsageSessionCreateOk(sid) {
      return request.resource.data.keys().hasOnly(iosUsageSessionKeys())
        && request.resource.data.keys().hasAll([
          'sessionId', 'deviceId', 'platform', 'appVersion', 'openedAt', 'events', 'lastEventName'
        ])
        && request.resource.data.sessionId == sid
        && iosUsageSessionIdOk(request.resource.data.sessionId, request.resource.data.deviceId)
        && request.resource.data.platform == 'ios'
        && request.resource.data.appVersion is string
        && request.resource.data.openedAt is string
        && request.resource.data.lastEventName is string
        && request.resource.data.events is list
        && request.resource.data.events.size() >= 1;
    }

    function iosUsageSessionUpdateOk(sid) {
      return request.resource.data.keys().hasOnly(iosUsageSessionKeys())
        && request.resource.data.keys().hasAll([
          'sessionId', 'deviceId', 'platform', 'appVersion', 'openedAt', 'events', 'lastEventName'
        ])
        && resource.data.sessionId == sid
        && request.resource.data.sessionId == resource.data.sessionId
        && request.resource.data.deviceId == resource.data.deviceId
        && request.resource.data.openedAt == resource.data.openedAt
        && request.resource.data.platform == resource.data.platform
        && request.resource.data.platform == 'ios'
        && iosUsageSessionIdOk(request.resource.data.sessionId, request.resource.data.deviceId)
        && request.resource.data.events is list
        && resource.data.events is list
        && request.resource.data.events.size() >= resource.data.events.size()
        && request.resource.data.lastEventName is string
        && request.resource.data.appVersion is string;
    }

    match /iosUsageEvents/{sessionId} {
      allow create: if iosUsageSessionCreateOk(sessionId);
      allow update: if iosUsageSessionUpdateOk(sessionId);
      allow read, delete: if false;
    }
  }
}
